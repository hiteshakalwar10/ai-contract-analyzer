name: Keep Streamlit App Awake

on:
  schedule:
    - cron: '0 0 */3 * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Streamlit app
        run: curl -s https://ai-contract-analyzer.streamlit.app > /dev/null
